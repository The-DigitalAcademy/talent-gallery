import { cn } from "../../lib/utils";

interface ExperienceCardProps {
  title: string;
  subTitle: string;
  description: string;
  bgColor: string;
  titleStyle: string;
  subTitleStyle: string;
  descriptionStyle: string;
  padding: string;
}

export default function ExperienceCard({ padding, title, subTitle, titleStyle, bgColor, subTitleStyle, description, descriptionStyle}: ExperienceCardProps) {
    return(
        <div>
            
        </div>
    )
}